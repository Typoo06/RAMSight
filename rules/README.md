# Rules

Configuration for YARA signatures, detection rules, and risk scoring.

Do not place malware samples or memory dumps here.

YARA rules for the memory-only malware demo live in `rules/yara/`. See `docs/YARA_RULES.md` for rule groups, required metadata, test commands, and guidance for interpreting YARA findings.
